//2. sort - use JS Array.sort implementation
//   sort(d)
// -> d an array of random numbers
// <- the random numbers sorted
// verify is sorted

let d=[];
for(i=0; i<10; i++){
d[i]=Math.floor(1+(Math.random()*20));
} 
document.write(d.sort((a,b)=>a-b));  
