 //2. set
//   attach(n)//wrap the method 'add'
// -> n random name
// <- random name added
// alone or proto
 
function randomn(){  
  return letra = String.fromCharCode(Math.floor(65+(Math.random()*25)));}
  
  let añadir=new Set();

for(i=0;i<5;i++)
	añadir.add(randomn()+randomn())
  
for (const valor of añadir) 
	console.log(valor);
