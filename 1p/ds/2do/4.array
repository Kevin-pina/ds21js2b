//4. array
//   attach(n)//wrap the method 'push'
// -> n random name
// <- random name added
// alone or proto

function randomn(){  
  return letra = String.fromCharCode(Math.floor(65+(Math.random()*25)));}
  
  let añadir=[];

for(i=0;i<5;i++)
	añadir.push(randomn()+randomn());
  
	console.log(añadir);
